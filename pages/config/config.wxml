<!--pages/two/two.wxml-->
<view>
    <view class="seciton-title">
    立论阶段
    <switch id="config1" class="pull-right" checked="{{configs.config1.state}}" bindchange="switchChange"/>
    </view>
    
        <view class="hr"></view>
    <view>
         <text>时间限制(秒)</text> 
        <slider id="config1" bindchange="sliderChange" min="10" max="360" value="{{configs.config1.time}}" show-value/>
    </view>
    <view>
         <text>声音提醒</text>
         <radio-group id="config1" class="voice" bindchange="radioChange">
  <!--<lable class="radio" wx:for="{{items}}">
    <radio value="{{item.name}}" checked="{{item.checked}}"/>{{item.value}}
  </lable>-->
            <label> <radio value="15"  checked="true">提前提前15秒</radio> </label>
            <label> <radio value="10">提前提前10秒</radio> </label>
            <label> <radio value="5">提前提前5秒</radio> </label>
        </radio-group>
    </view>
</view>

<view>
    <view class="seciton-title">
    <text>驳立论阶段</text>
    <switch id="config2" class="pull-right" checked="{{configs.config2.state}}" bindchange="switchChange"/>
    </view>
    
        <view class="hr"></view>
    <view>
         <text>时间限制(秒)</text> 
        <slider id="config2" bindchange="sliderChange" min="10" max="360" value="{{configs.config2.time}}" show-value/>
    </view>
    <view>
         <text>声音提醒</text>
         <radio-group id="config2" class="voice" bindchange="radioChange">
  <!--<lable class="radio" wx:for="{{items}}">
    <radio value="{{item.name}}" checked="{{item.checked}}"/>{{item.value}}
  </lable>-->
            <label> <radio value="15"  checked="true">提前提前15秒</radio> </label>
            <label> <radio value="10">提前提前10秒</radio> </label>
            <label> <radio value="5">提前提前5秒</radio> </label>
        </radio-group>
    </view>
</view>

<view>
    <view class="seciton-title">
    质变环节
    <switch id="config3" class="pull-right" checked="{{configs.config3.state}}" bindchange="switchChange"/>
    </view>
    
        <view class="hr"></view>
    <view>
         <text>时间限制(秒)</text> 
        <slider id="config3" bindchange="sliderChange" min="10" max="360" value="{{configs.config3.time}}" show-value/>
    </view>
    <view>
         <text>声音提醒</text>
         <radio-group id="config3" class="voice" bindchange="radioChange">
  <!--<lable class="radio" wx:for="{{items}}">
    <radio value="{{item.name}}" checked="{{item.checked}}"/>{{item.value}}
  </lable>-->
            <label> <radio value="15"  checked="true">提前提前15秒</radio> </label>
            <label> <radio value="10">提前提前10秒</radio> </label>
            <label> <radio value="5">提前提前5秒</radio> </label>
        </radio-group>
    </view>
</view>

<view>
    <view class="seciton-title">
    自由辩论
    <switch id="config4" class="pull-right" checked="{{configs.config4.state}}" bindchange="switchChange"/>
    </view>
    
        <view class="hr"></view>
    <view>
         <text>时间限制(秒)</text> 
        <slider id="config4" bindchange="sliderChange" min="10" max="360" value="{{configs.config4.time}}" show-value/>
    </view>
    <view>
         <text>声音提醒</text>
         <radio-group id="config4" class="voice" bindchange="radioChange">
                    <!--<lable class="radio" wx:for="{{items}}">
                        <radio value="{{item.name}}" checked="{{item.checked}}"/>{{item.value}}
                    </lable>-->
            <label> <radio value="15" checked="true">提前提前15秒</radio> </label>
            <label> <radio value="10">提前提前10秒</radio> </label>
            <label> <radio value="5">提前提前5秒</radio> </label>
        </radio-group>
    </view>

    <view class="seciton-title">
    总结陈词
    <switch id="config5" class="pull-right" checked="{{configs.config5.state}}" bindchange="switchChange"/>
    </view>
    
        <view class="hr"></view>
    <view>
         <text>时间限制(秒)</text> 
        <slider id="config5" bindchange="sliderChange" min="10" max="360" value="{{configs.config5.time}}" show-value/>
    </view>
    <view>
         <text>声音提醒</text>
         <radio-group id="config5" class="voice" bindchange="radioChange">
                    <!--<lable class="radio" wx:for="{{items}}">
                        <radio value="{{item.name}}" checked="{{item.checked}}"/>{{item.value}}
                    </lable>-->
            <label> <radio value="15" checked="true">提前提前15秒</radio> </label>
            <label> <radio value="10">提前提前10秒</radio> </label>
            <label> <radio value="5">提前提前5秒</radio> </label>
        </radio-group>
    </view>

</view>